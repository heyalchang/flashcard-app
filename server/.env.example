# Backend Environment Variables
# ================================

# Server Configuration
PORT=3051
WS_PORT=3051
NODE_ENV=development

# PipeCat Cloud API (Backend Only - Never expose to frontend!)
PIPECAT_API_KEY=sk_your-pipecat-api-key-here
PIPECAT_AGENT_NAME=my-first-agent
# Note: API URL is fixed to https://api.pipecat.daily.co/v1

# Webhook URL for PipeCat Agent
# Development: Use ngrok URL (run: ngrok http 3051)
WEBHOOK_URL=https://your-ngrok-id.ngrok-free.app/webhook
# Production: Use your deployed backend URL
# WEBHOOK_URL=https://your-app.elasticbeanstalk.com/webhook

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
# Production: Update with your frontend domain
# CORS_ORIGIN=https://your-frontend.vercel.app

# Optional: Daily API (if managing rooms directly)
# DAILY_API_KEY=your-daily-api-key
# DAILY_DOMAIN=your-daily-domain

# IMPORTANT: Session timeout is hardcoded to 10 minutes for cost control
# Each active session costs ~$1.40/hour if left running!